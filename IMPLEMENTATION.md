# 實作細節與設計取捨

寫給未來要改這個 repo 的你。每一節都是「我做了什麼決定、為什麼、代價是什麼」。

---

## 0. 一個先講的環境限制

開發環境（雲端容器和你電腦的 Cowork VM）**對外網路是白名單制**，實測結果：

| 主機 | 雲端容器 | 你電腦的 VM |
|---|---|---|
| `www.travel.taipei` | 403（政策拒絕） | ✗ |
| `data.gov.tw` / `trendy.taipei` / `meetup.com` / `accupass.com` / `kktix.cc` | ✗ | ✗ |
| `github.com` / `pypi.org` | ✓ | ✗ |

**影響**：collector 沒辦法在這裡跑。這不是 bug，是網路政策。

**因此架構上做了兩件事**：

1. 執行位置定在 **GitHub Actions**（runner 有完整外網）。原本「先用 Cowork 排程起步、之後再搬到 Actions」的建議在這個網路政策下不成立，直接跳過中間步驟。
2. 所有邏輯都做成**離線可測**。`tests/test_pipeline.py` 用 `tests/fixtures/` 裡錄下來的樣本，把「對應 → 寫入 → upsert → 查詢」整條跑完，不碰網路。

臺北旅遊網的欄位名稱是從官方 Swagger 文件確認的，不是猜的。但**回應的最外層包裝沒有文件**，所以 `Collector.unwrap()` 會試幾個常見的鍵再退回「找第一個 list of dict」。第一次在 Actions 上跑之前，先跑 `--probe` 看真實結構。

---

## 1. 為什麼是 jsonl，不是 SQLite

這張表要 commit 進 git，你才能用 `git diff` 直接看到「這週多了什麼活動」。
jsonl 一行一筆，diff 乾淨；SQLite 是 binary，diff 完全看不懂。

**代價**：每次都要整檔重寫。到 5 萬筆左右會開始慢，那時候再換。

配套：`store.save()` 寫入前**依 `(starts_at, uid)` 排序**。沒有這個，dict 順序變動會讓每次 commit 都是一大片假變更，git diff 就廢了。

---

## 2. 主鍵與去重

**主鍵 = `source_platform:source_id`**。同平台同 id 就是同一筆。

**跨平台刻意不合併。** 同一場音樂節在臺北旅遊網和潮臺北都出現，資料表裡就是兩筆。

理由：合併是破壞性的，一旦合錯就救不回來；而且兩邊的資訊常常互補（一邊有票價、一邊有交通）。所以只提供 `Event.dedup_key`（正規化標題 + 開始日）當「可能是同一場」的提示，**在查詢時分組，不在寫入時刪資料**。

代價：推薦清單初期會有重複。看到了再處理，比先寫一套會誤刪的合併邏輯安全。

---

## 3. upsert：哪些欄位不能被覆蓋

`store.py` 裡的 `_PRESERVED = ("first_seen", "feedback")`。

沒有這道防線，下一次抓取會把你標的 feedback 洗掉，`first_seen` 也會變成最新時間 —— 「這週新出現的」就永遠算不出來。這是這類專案最常見的靜默 bug：不會報錯，只是功能默默失效。

`tests/test_pipeline.py` 第 3 節專門測這件事。

**變更偵測用 `content_hash`**，只 hash 會實質改變活動的欄位（標題、時間、地點、票價、狀態）。不 hash 整包原始資料 —— 否則來源加個追蹤參數就判定成「有更新」。

---

## 4. 時間處理

**規則：資料表裡不存 naive datetime，一律帶 `+08:00`。** `schema.validate()` 會擋。

來源幾乎都給裸的本地時間，沒有時區。`to_taipei_iso()` 沒看到時區就當台北時間。

**一個容易踩的細節**：只有日期沒有時間的「結束日」要補到 `23:59:59`，不是 `00:00`。
臺北旅遊網的 `end: "2026-09-21"` 如果補成 00:00，這場活動在 9/21 一早就會被標成 expired，你當天就看不到它了。所以 `to_taipei_iso(..., end_of_day=True)`。

---

## 5. `signup_deadline` 的三態問題

情境 2 要「還沒截止」的活動，但**臺北旅遊網不提供報名截止日**，多數來源也不給。

`signup_deadline = None` 的意思是「**來源沒給**」，不是「沒有截止日」。這兩件事不一樣。

`query(signup_open=True)` 的處理：deadline 是 None 時退而求其次用開始時間判斷。這會漏掉「活動下週但報名昨天就截止」的情況 —— 這是已知的不準，輸出時應該標注「截止日未知」，不要假裝確定。

同樣邏輯用在 `is_free`：`True` / `False` / `None`（不知道）三態。**不要把 None 當成 False。**

---

## 6. 分類（TYPE_RULES）—— 最需要你調的地方

`normalize.py` 的 `TYPE_RULES` 是一張由上往下比對的關鍵字表，第一個命中的勝出。所以**窄的規則要放前面**：「英文角」必須比「講座」先比中，否則會被歸成 `TALK`，情境 2 就抓不到它。

**分不出來一律 `OTHER`，不猜。** 猜錯比 `OTHER` 糟：猜錯會讓活動出現在錯的情境裡（你會開始不信任推薦），`OTHER` 只是暫時看不到（你會去翻）。

這是全專案最需要人工調校的部分。**覺得推薦爛的時候，先改這裡和 `interests.yaml`，再考慮加來源。**

語言另外用 tag 標（`lang:en`、`lang:ja`），因為 type 只有一個值，但「英文的語言交流」需要兩個維度。

---

## 7. 失敗隔離

三層：

1. **collector 層** — `Collector.run()` 吃掉所有例外，回 `CollectorResult(ok=False)`。一支炸掉不影響其他支。
2. **單筆層** — `schema.validate()` 回報問題清單而不丟例外。一筆壞資料不該讓整次抓取失敗，但也不該安靜混進資料表 —— runner 會把前 5 筆問題印出來。
3. **檔案層** — `store.load()` 跳過讀不進來的行；`store.save()` 先寫暫存檔再 `os.replace()` 原子換上。中途被砍不會留下半個檔案。

**Actions 的紅綠燈規則**：部分來源失敗 → 綠燈（有資料總比沒有好）；**全部**來源失敗 → 紅燈。否則它壞了三週你都不會發現。

---

## 8. 分頁防呆

`_fetch_endpoint` 用 `seen_ids` 記已抓過的 id，一頁全是舊 id 就停。

有些分頁實作超過最後一頁時會一直回最後一頁的內容，沒有這個防呆會空轉到 `max_pages`（20 頁）才停，每頁還 sleep 1 秒。

`delay_between_pages = 1.0` 是對來源的禮貌。你一週只跑一次，沒必要打快。

---

## 9. 官方欄位的拼字錯誤

臺北旅遊網 Swagger 裡有兩個拼錯的欄位名，**是他們的錯，不要「順手修正」**：

- `distric`（少一個 t）
- `co_rganizer`（應為 co_organizer）

改掉就直接讓對應失效，而且不會報錯 —— 只會安靜地全部變成 None。

---

## 10. 還沒做的

- **潮臺北 / Meetup collector** —— 需要先在有外網的環境跑 `--probe` 看實際結構。潮臺北要解析 HTML（比 API 脆弱，改版就壞）；Meetup 的 GraphQL 要 OAuth。
- **`interests.yaml` 還沒被程式讀** —— 目前只是規格。`boost` / `exclude` 的排序邏輯要跟 skill 一起寫。
- **回饋迴路** —— `feedback` 欄位已經預留並保護好了，但還沒有讓你標記的介面。這是決定「第 8 週你還會不會用」的關鍵，優先度應該高於加來源。
- **IG / FB / Threads** —— 依你的決定先跳過。附帶一提，Threads 官方 API 只能讀你自己授權帳號的內容，讀別人的公開貼文不在功能範圍內，所以那條路不是「還沒做」，是「官方不給做」。

---

## 12. 新增來源的標準流程：取得方式的階梯

每加一個來源，**由上往下試，能停在上面就停**。這不是技術優劣排名，
是「離對方主動公開的意圖有多遠」的排名。

| 層 | 取得方式 | 對方有沒有動機維持 |
|---|---|---|
| 1 | JSON-LD / schema.org 結構化資料 | 有 —— 弄壞了 Google 搜尋結果就掉 |
| 2 | 網站自己的內部 JSON API | 有 —— 弄壞了自己的前端就爆 |
| 3 | sitemap.xml（發現網址用） | 有 —— 標準格式，搜尋引擎在看 |
| 4 | RSS / Atom | 有 —— 它存在的目的就是給機器讀 |
| 5 | 解析 HTML + CSS selector | **沒有** —— 改版對他們只是調樣式 |
| 6 | 無頭瀏覽器（Playwright） | 沒有，且成本最高 |

### 為什麼是這個順序

**一、契約穩定度（最重要）**
關鍵不是「哪種比較好寫」，是**改動時誰會先痛**。第 1–4 層的共同點是
對方弄壞了自己也會痛，所以有動機不亂改。第 5 層是純粹的單向依賴：
他們調個 class name 對自己毫無影響，你單方面壞掉。

**二、語意保真度**
`"startDate": "2026-09-19T19:00:00+08:00"` 是明確的型別與語意。
`<span class="text-sm">9/19 (五) 19:00</span>` 要你自己反推。
每往下一層，要寫的猜測邏輯就變多，而猜測邏輯就是 bug 的所在。

**三、失敗模式（最被低估）**
- 上層壞掉會**爆**：欄位不見、JSON parse 失敗 → 你會知道
- 下層壞掉會**默默給錯的東西**：selector 匹配到別的元素，一路寫進資料表 → 你不會知道

travel_taipei 已經示範過一次：`address` 全 25 筆是空的，程式完全沒報錯。
那還只是 API 少給欄位。如果是 selector 抓錯，連「這欄該有值」都不會知道。

→ 推論：**越往下層，越需要主動的資料健檢**。不只是 try/except，
而是「這欄空值率從 5% 跳到 90% 要告警」。

**四、成本（排第四，不是第一）**
一週跑一次、幾百筆的規模下，100ms vs 5 秒 = 30 秒 vs 25 分鐘，都可接受。
**性能幾乎不構成排序理由。** Playwright 真正的成本是 300MB 依賴、
CI 版本問題、吃 Actions 額度，不是那幾秒。

**五、可重用性**
第 1–4 層的解析器**跨網站通用**：寫一支 JSON-LD 解析器，
所有有 JSON-LD 的站都能用，加第 N+1 個來源接近零成本。
第 5 層每個網站都是獨一無二的手工藝品，零重用。
這個專案的路線圖就是「一直加來源」，所以這條特別重要。

**六、封鎖風險**
越往下越像 bot，越容易被擋。

### 一個重要推論

**需要 Playwright 通常代表偵查沒做完。** 內容是 JS 算出來的 → JS 一定打了某支
API → 第 2 層存在，只是還沒找到。Playwright 是那支 API 真的找不到
（或需要複雜登入狀態）時的退路，不是預設選項。

### 每加一個來源的偵查順序

1. 讀 `<網域>/robots.txt` —— 確認可不可以、哪些路徑可以、crawl-delay 多少
2. 開一個列表頁，搜 `application/ld+json`
3. 開發者工具 Network → 篩 Fetch/XHR → 找回傳活動資料的那支
4. 試 `/sitemap.xml`（robots.txt 通常也會列出位置）
5. 找 RSS/Atom
6. 以上都沒有，才考慮解析 HTML

四步做完才決定 collector 怎麼寫。跳過偵查直接寫 selector 是這類專案
最常見的自傷。

---

## 11. 下一步的建議順序

1. push 到 GitHub，在 Actions 頁面手動觸發一次 `workflow_dispatch`
2. 看 log 裡抓到幾筆、`invalid` 幾筆
3. 用 `--probe` 的輸出核對外層包裝，需要的話修 `unwrap`
4. 翻一遍 `events.jsonl`，看 `type` 分類錯多少 → 回頭調 `TYPE_RULES`
5. 分類穩定了，再寫 skill
6. skill 能用了，再加第二個來源

**不要在第 4 步之前加來源。** 分類不準的時候加來源只是讓錯誤變多。
